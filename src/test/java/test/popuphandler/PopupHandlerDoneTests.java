package test.popuphandler;

import helpers.BaseTest;
import org.testng.Assert;
import org.testng.annotations.Test;

public class PopupHandlerDoneTests extends BaseTest {
    @Test
    public void testPopupHandlerDone() {
        Assert.assertTrue(true);
    }
}
package test.authentication;

import helpers.BaseTest;
import org.testng.Assert;
import org.testng.annotations.Test;

public class AuthenticationDoneTests extends BaseTest {
    @Test
    public void testAuthenticationDone() {
        Assert.assertTrue(true);
    }
}

